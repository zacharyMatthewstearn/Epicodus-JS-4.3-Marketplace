
import { helperCart } from 'marketplace/helpers/helper-cart';
import { module, test } from 'qunit';

module('Unit | Helper | helper cart');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = helperCart([42]);
  assert.ok(result);
});

